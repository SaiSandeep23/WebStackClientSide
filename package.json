{
  "devDependencies": {
    "node-sass": "6.0"
  },
  "scripts": {
    "scss": "node-sass styles.scss style_map.css",
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite": "lite-server"
  }
}